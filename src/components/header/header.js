import './header.css'
import Navbar from "../navbar/navbar";

function Header() {
    return (
        <header>
            <Navbar/>
            <p>Header</p>
        </header>
    );
}

export default Header;